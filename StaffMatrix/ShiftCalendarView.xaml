<UserControl x:Class="StaffMatrix.ShiftCalendarView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">

    <Grid Margin="10">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="250"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Calendar Control -->
        <Calendar x:Name="shiftCalendar"
                  Grid.Column="0"
                  SelectedDatesChanged="shiftCalendar_SelectedDatesChanged"
                  Margin="0,0,10,0"/>

        <!-- Shift Details -->
        <Border Grid.Column="1" BorderThickness="1" BorderBrush="Gray" Padding="10">
            <StackPanel>
                <TextBlock Text="Shifts for Selected Date" 
                           FontWeight="Bold" FontSize="16" Margin="0,0,0,10"/>
                <ListBox x:Name="ShiftListBox" Height="300"/>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
