<UserControl x:Class="StaffMatrix.EmployeeManagementView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">

    <!-- Added ScrollViewer for horizontal scrolling -->
    <ScrollViewer HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto">
        <Grid Margin="10">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Employee Input Fields -->
            <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                <StackPanel Margin="0,0,10,0">
                    <TextBlock Text="First Name"/>
                    <TextBox x:Name="txtFirstName" Width="120"/>
                </StackPanel>
                <StackPanel Margin="0,0,10,0">
                    <TextBlock Text="Last Name"/>
                    <TextBox x:Name="txtLastName" Width="120"/>
                </StackPanel>
                <StackPanel Margin="0,0,10,0">
                    <TextBlock Text="Role"/>
                    <TextBox x:Name="txtRole" Width="120"/>
                </StackPanel>
                <StackPanel Margin="0,0,10,0">
                    <TextBlock Text="Email"/>
                    <TextBox x:Name="txtEmail" Width="150"/>
                </StackPanel>
                <StackPanel Margin="0,0,10,0">
                    <TextBlock Text="Phone"/>
                    <TextBox x:Name="txtPhone" Width="120"/>
                </StackPanel>
                <Button Content="Add Employee" Width="120" VerticalAlignment="Bottom" Click="AddEmployee_Click"/>
            </StackPanel>

            <!-- Employee Grid -->
            <DataGrid x:Name="employeeGrid" Grid.Row="1" AutoGenerateColumns="False" CanUserAddRows="False">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="ID" Binding="{Binding EmployeeID}" Width="50"/>
                    <DataGridTextColumn Header="First Name" Binding="{Binding FirstName}" />
                    <DataGridTextColumn Header="Last Name"  Binding="{Binding LastName}" />
                    <DataGridTextColumn Header="Role"       Binding="{Binding Role}" />
                    <DataGridTextColumn Header="Email"      Binding="{Binding Email}" />
                    <DataGridTextColumn Header="Phone"      Binding="{Binding Phone}" />
                    <DataGridTemplateColumn Header="Actions" Width="100">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Button Content="Remove" Click="RemoveEmployee_Click" Tag="{Binding}"/>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>
        </Grid>
    </ScrollViewer>
</UserControl>
